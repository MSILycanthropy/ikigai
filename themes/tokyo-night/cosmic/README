builder.ron — Tokyo Night as a COSMIC ThemeBuilder. Import via Settings → Desktop → Appearance → Import.
No comments allowed in the file: cosmic-settings 1.7 rejects a leading `//`.
The built CosmicTheme.Dark/v2 files here are harvested from a VM after import.
