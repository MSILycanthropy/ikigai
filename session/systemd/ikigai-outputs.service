[Unit]
Description=Ikigai outputs (keeps the display layout across the monitors' sleep)
PartOf=ikigai-session.target
After=graphical-session-pre.target

[Service]
ExecStart=/usr/local/bin/ikigai-outputs
Restart=on-failure
RestartSec=1
