# Vicinae disables layer-shell when XDG_CURRENT_DESKTOP contains "cosmic" (cosmic-comp#1590);
# the patched Qt makes it safe, so give this process a different desktop name.
[Service]
Environment=XDG_CURRENT_DESKTOP=Ikigai QT_QPA_PLATFORM=wayland
RestartSec=1
