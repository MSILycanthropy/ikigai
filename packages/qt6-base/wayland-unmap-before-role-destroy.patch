--- a/src/plugins/platforms/wayland/qwaylandwindow.cpp	2026-09-01 17:57:35.831813494 -0500
+++ b/src/plugins/platforms/wayland/qwaylandwindow.cpp	2026-09-01 17:57:35.847449181 -0500
@@ -654,9 +654,12 @@
         // make sure isExposed is false during the next event dispatch
         mExposed = false;
         sendExposeEvent(QRect());
-        resetSurfaceRole();
+        // Unmap the surface while it still has its role. Committing to a
+        // surface whose role object was already destroyed is rejected by
+        // some compositors (Smithay/COSMIC), which drop the connection.
         mSurface->attach(nullptr, 0, 0);
         mSurface->commit();
+        resetSurfaceRole();
     }
 }
 
