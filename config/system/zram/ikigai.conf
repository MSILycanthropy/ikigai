# Compressed swap in RAM. zstd averages about 3:1, so a full zram0 sized to the whole of
# RAM still occupies about a third of it; anything smaller spills reclaim to disk.
[zram0]
zram-size = ram
compression-algorithm = zstd
swap-priority = 100
