# A stuck service holds shutdown for 90 s by default; five is enough for anything that
# was going to stop at all. docker.service keeps its own, longer timeout (docker.service.d).
[Manager]
DefaultTimeoutStopSec=5s
