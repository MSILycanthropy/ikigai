# Notice a dropped connection within a minute instead of sitting on a hung terminal until
# TCP gives up. ~/.ssh/config is read first and wins per host.
Host *
  ServerAliveInterval 15
  ServerAliveCountMax 3
  ConnectTimeout 10
